<nav class="navbar navbar-expand-lg {#navbar-light bg-light #}">
  <img src="{{ asset('build/images/navbar.png')}}" alt="image-navbar" height="120">
  <a id="navbar-brand" class="navbar-brand" href="{{ path("user_index") }}">Accueil</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Choisis ton sportif
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          {% for user in users %}

            <a class="dropdown-item" href="{{ path('user_show', {'id' : user.id }) }}">{{ user.firstname }}</a>
          {% endfor %}
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Tous les évènements
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          {% for event in events %}
            <a class="dropdown-item" href="{{ path('event_show', {'id' : event.id }) }}">{{ event.name }}</a>
          {% endfor %}
        </div>
      </li>
    </ul>
  </div>
</nav>